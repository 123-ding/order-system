# 石狮市低空经济飞行审批流程

本文档描述石狮市低空经济服务平台飞行审批全流程，涵盖飞行活动审批（石狮市交港局 运输安全股）、放飞审批（石狮市公安局 治安管理大队）及军方对接逻辑。

> **渲染说明**：菱形节点只保留"节点名称 + 审批部门"，避免节点文字过长导致截断。完整职责说明统一列于「职责说明图」及下方表格，通过独立图保证节点完整可见、不被遮挡。
> 由于 GitHub Mermaid 使用 dagre 布局引擎，将职责旁路节点放入复杂 TD 主流程图会导致其被布局到画布边缘或与其他节点重叠。因此职责说明单独采用 `flowchart LR` 小图（仅3对节点），彻底消除遮挡风险。
>
> ⚠️ **维护注意**：「职责说明图」和「审批节点职责汇总表」中的职责描述内容相同，如需修改职责请同步更新两处。

---

## 一、总流程图

```mermaid
flowchart TD
    A([用户提交飞行活动申请]) --> B{飞行活动类型判断}
    B --> C[一般飞行活动]
    B --> D[长期飞行活动]
    B --> E[紧急飞行活动]
    B --> F[场内飞行活动]

    C --> G[飞行活动审批]
    D --> G
    E --> G
    F --> G

    G --> H{审批节点1-初审<br/>交港局 运输安全股}
    H -->|不通过| R1([审批驳回，通知申请人])
    H -->|通过| I{审批节点2-复审<br/>交港局 运输安全股}
    I -->|不通过| R2([审批驳回，通知申请人])
    I -->|通过| J([飞行活动审批通过])

    J --> K[计划起飞1小时前<br/>提交放飞申请]

    K --> X{是否长期飞行活动}
    X -->|否-非长期| MA[军方备案<br/>记录回执/编号/时间]
    MA --> N([允许起飞<br/>起飞信息同步上报])

    X -->|是-长期| MB[军方审核/放行<br/>适用：长期飞行活动]
    MB --> MC{军方结论}
    MC -->|不同意/限制| R4([军方未放行<br/>不得起飞/调整计划])
    MC -->|同意/放行| L{放飞审批节点<br/>公安局 治安管理大队}
    L -->|不通过| R3([放飞申请驳回，通知申请人])
    L -->|通过| N

    style A fill:#4CAF50,color:#fff
    style J fill:#2196F3,color:#fff
    style N fill:#4CAF50,color:#fff
    style R1 fill:#f44336,color:#fff
    style R2 fill:#f44336,color:#fff
    style R3 fill:#f44336,color:#fff
    style R4 fill:#f44336,color:#fff
```

---

## 二、职责说明图

> 以下独立图仅展示各审批节点与其完整职责的虚线关联，与主流程图分离，确保职责节点完整可见、不被遮挡。

```mermaid
flowchart LR
    H["审批节点1-初审<br/>交港局 运输安全股"]
    Hx["职责（初审）<br/>1. 受理申请<br/>2. 材料完整性校验<br/>3. 合规初筛<br/>4. 风险标注<br/>输出：通过 / 驳回 / 补正"]
    H -. 职责说明 .-> Hx

    I["审批节点2-复审<br/>交港局 运输安全股"]
    Ix["职责（复审）<br/>1. 空域/时间冲突审查<br/>2. 敏感空域复核<br/>3. 审批条件设置<br/>4. 归档留痕<br/>输出：批准（含条件）/ 不批准"]
    I -. 职责说明 .-> Ix

    L["放飞审批节点<br/>公安局 治安管理大队"]
    Lx["职责（放飞确认）<br/>1. 核对已批及条件满足<br/>2. 起飞前1小时信息确认<br/>3. 临时管制与空域动态检查<br/>4. 最终放飞确认<br/>5. 记录留痕与上报<br/>输出：允许起飞 / 不得起飞"]
    L -. 职责说明 .-> Lx

    style Hx fill:#fff9c4,stroke:#f9a825
    style Ix fill:#fff9c4,stroke:#f9a825
    style Lx fill:#fff9c4,stroke:#f9a825
```

---

## 三、审批节点职责汇总表

| 审批节点 | 审批部门 | 完整职责 | 输出 |
|---------|---------|---------|------|
| **审批节点1 — 初审** | 石狮市交港局 运输安全股 | 1. 受理申请；2. 材料完整性校验；3. 合规初筛；4. 风险标注 | 通过 / 驳回 / 补正 |
| **审批节点2 — 复审** | 石狮市交港局 运输安全股 | 1. 空域/时间冲突审查；2. 敏感空域复核；3. 审批条件设置；4. 归档留痕 | 批准（含条件）/ 不批准 |
| **放飞审批节点** | 石狮市公安局 治安管理大队 | 1. 核对已批及条件满足；2. 起飞前1小时信息确认；3. 临时管制与空域动态检查；4. 最终放飞确认；5. 记录留痕与上报 | 允许起飞 / 不得起飞 |
| **军方备案**（非长期） | 军方（解放军/空军相关部门） | 非长期飞行活动，不阻断主流程，提交备案并取得回执编号 | 备案回执/编号/时间 |
| **军方审核/放行**（长期） | 军方（解放军/空军相关部门） | 长期飞行活动，阻断式等待军方明确放行指令 | 同意放行 / 不同意 / 限制条件 |

---

## 四、飞行活动审批细化图

```mermaid
flowchart TD
    subgraph 申请阶段
        A([申请人/单位]) --> B[登录石狮市低空经济服务平台]
        B --> C[填写飞行活动申请]
        C --> D{选择活动类型}
        D --> D1[一般飞行活动]
        D --> D2[长期飞行活动]
        D --> D3[紧急飞行活动]
        D --> D4[场内飞行活动]
    end

    D1 --> E
    D2 --> E
    D3 --> E
    D4 --> E

    subgraph 飞行活动审批["飞行活动审批（石狮市交港局 运输安全股）"]
        E[提交申请] --> F{审批节点1 — 初审<br/>交港局 运输安全股}
        F -->|通过| G{审批节点2 — 复审<br/>交港局 运输安全股}
        F -->|驳回/补正| F1([退回申请人<br/>说明原因])
        G -->|批准| H([飞行活动审批通过])
        G -->|不批准| G1([退回申请人<br/>说明原因])
    end

    style A fill:#4CAF50,color:#fff
    style H fill:#2196F3,color:#fff
    style F1 fill:#f44336,color:#fff
    style G1 fill:#f44336,color:#fff
```

**飞行活动审批节点职责（见「职责说明图」及汇总表）**

---

## 五、放飞审批细化图（含军方对接）

```mermaid
flowchart TD
    subgraph 前置条件
        A([飞行活动已获批准]) --> B[计划起飞1小时前]
    end

    B --> C[提交放飞申请<br/>报告预计起飞时刻和准备情况]

    C --> X{是否长期飞行活动}

    X -->|否-非长期| MA[军方备案<br/>记录回执/编号/时间]
    MA --> E([允许起飞<br/>起飞信息同步上报])

    X -->|是-长期| MB[军方审核/放行<br/>适用：长期飞行活动]
    MB --> MC{军方结论}
    MC -->|不同意/限制| R2([军方未放行<br/>不得起飞/调整计划])
    MC -->|同意/放行| D{放飞审批节点<br/>公安局 治安管理大队}

    D -->|通过| E
    D -->|不通过| F([放飞申请驳回<br/>不得起飞])

    style A fill:#2196F3,color:#fff
    style E fill:#4CAF50,color:#fff
    style F fill:#f44336,color:#fff
    style R2 fill:#f44336,color:#fff
```

**放飞审批节点职责（见「职责说明图」及汇总表）**

---

## 六、纵向上报流程图

```mermaid
flowchart TD
    A([申请人提交申请]) --> B[石狮市平台受理]
    B --> C{活动类型与空域判断}

    C -->|石狮市权限范围内| SS[石狮市交港局<br/>运输安全股 审批节点1+节点2]
    C -->|跨区域/特殊空域/大型活动| SS1{石狮初审<br/>交港局 运输安全股}
    SS1 -->|同意上报| E[上报泉州市平台]
    SS1 -->|退回补正| A

    SS -->|审批通过| F[审批结果同步上报<br/>泉州 → 福建 → UOM]
    SS -->|审批驳回| R1([驳回通知申请人])

    E --> G{泉州市平台审批}
    G -->|需省级协调| H[上报福建省平台]
    G -->|泉州市权限内| I[泉州市审批]

    H --> J{福建省平台审批}
    J -->|需国家级协调| K[上报UOM平台<br/>空军/民航协调]
    J -->|省级权限内| L[福建省审批]

    K --> M[UOM协调空管机构]
    M --> N([审批结果逐级下达])

    L --> N
    I --> N
    F --> O([审批通过])
    N --> O

    O --> P[计划起飞1小时前<br/>提交放飞申请]
    P --> QX{是否长期飞行活动}

    QX -->|否-非长期| QMA[军方备案<br/>登记回执/编号/时间]
    QMA --> R([允许起飞<br/>信息同步上报UOM])

    QX -->|是-长期| QMB[军方审核/放行<br/>适用：长期飞行活动]
    QMB --> QMC{军方结论}
    QMC -->|不同意/限制| S4([军方未放行<br/>不得起飞])
    QMC -->|同意/放行| Q{放飞审批<br/>公安局 治安管理大队}
    Q -->|通过| R
    Q -->|不通过| S([不得起飞])

    style A fill:#4CAF50,color:#fff
    style O fill:#2196F3,color:#fff
    style R fill:#4CAF50,color:#fff
    style R1 fill:#f44336,color:#fff
    style S fill:#f44336,color:#fff
    style S4 fill:#f44336,color:#fff
```

---

## 布局修复说明

| 问题 | 修复策略 |
|------|---------|
| 职责说明节点（初审/复审）在主流程图中被遮挡 | 将职责说明节点从主流程图中分离，放入独立的「职责说明图」（`flowchart LR`，仅3对节点），彻底消除与主流程的布局冲突 |
| 菱形节点文字过长导致截断 | 菱形节点只保留"节点名称 + 审批部门"，完整职责移至职责说明图和汇总表 |
| 军方逻辑与放飞流程关系不清 | 军方判断节点插入"提交放飞申请"之后：非长期→备案（不回公安）；长期→军方审核→公安放飞审批 |
